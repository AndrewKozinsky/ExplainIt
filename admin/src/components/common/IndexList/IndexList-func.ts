// import React from 'react'

/**
 * Обработчик щелчка по кнопке в пункте списка.
 * Нужен для остановки всплытия чтобы при нажатии на кнопку не выделялся пункт списка.
 * @param {Function} fn — дополнительная функция вызываемая при щелчке.
 */
/*export function onControlClick(fn: Function) {
	return function (e: React.MouseEvent) {
		e.stopPropagation()
		fn()
	}
}*/

export {}
