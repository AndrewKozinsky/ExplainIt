generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:kxPQor_cf23GR@0.0.0.0:4001/explainIt?schema=public"
}

model Article {
  id             Int     @default(autoincrement()) @db.SmallInt
  name           String  @unique @db.VarChar(255)
  article_number String  @unique @db.VarChar(100)
  published      Boolean
  summary        String  @unique @db.VarChar(255)
  content        String  @unique
  order_number   Int     @unique @db.SmallInt
}
