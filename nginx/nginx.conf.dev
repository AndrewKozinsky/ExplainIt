events {}

http {
    include mime.types;

    server {
        listen 80;
        server_name explain.local;

        location /api {
            proxy_pass http://api:3002;
            rewrite ^\/api\/(.*) /$1 break;
        }

        location /test {
            return 200 'Hello from Test';
        }
    }
}
